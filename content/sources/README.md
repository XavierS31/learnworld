# Curriculum sources

Place licensed CS1/CS2 PDFs here for local preprocessing. PDFs are intentionally ignored and never bundled into the application. Reviewed, normalized JSON belongs in `content/curriculum/`.
